const valor1 = 10.5234234
const valor2 = 25.7321312
const media = (valor1 + valor2) / 2;

//O método tofixed passado sobre uma variável de ponto flutuante reduz as casas decimais a direita para o tamanho que deseja exibir em tela
console.log(media.toFixed(2));