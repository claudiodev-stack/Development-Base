/* Funciona parecido com o querySelector, mas só retorna a referência do elemento através do seu ID.
Ps: não é necessário passar a referência de "#" no getElementById*/
const buscarElementoId = document.getElementById("curso");
console.log(buscarElementoId);